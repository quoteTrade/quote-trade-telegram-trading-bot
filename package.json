{
  "name": "quote-trade-telegram-trading-bot",
  "version": "1.0.0",
  "type": "commonjs",
  "license": "MIT",
  "description": "quote trade telegram trading bot with client API",
  "main": "dist/bot.js",
  "scripts": {
    "dev": "ts-node src/bot.ts",
    "build": "tsc -p .",
    "start": "node dist/bot.js",
    "test": "npm run build && node --test --experimental-test-coverage --test-coverage-exclude=\"tests/**\" --test-coverage-lines=80 --test-coverage-branches=80 --test-coverage-functions=80 \"tests/**/*.test.js\""
  },
  "dependencies": {
    "axios": "^1.10.0",
    "dotenv": "^17.2.0",
    "ethers": "^6.16.0",
    "numeral": "^2.0.6",
    "ws": "^8.20.1"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@types/ws": "^8.18.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "overrides": {
    "ws": "^8.20.1"
  },
  "engines": {
    "node": ">=22"
  }
}
